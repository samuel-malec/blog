{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Diplomka proposal","text":"<p>This document serves as a brief introduction into the concepts this project is concerned with, while also serving as my personal notes.</p>"},{"location":"#motivation","title":"Motivation","text":"<p>Whole-program inter-procedural analyses are difficult to implement efficiently. This is simply due to the sheer scope on which the analyses operate. Modern compilers therefore rely on local analyses combined with function summaries that approximate the behaviour of callees. That is the reason why we are trying to find a way that would make inter-procedural analyses slightly easier to reason about.</p> <p>Essentially, we would like to find a suitable way to extract and represent side-effect information of functions. If we would be able to do that, in theory we could create comprehensive function summaries based on the discovered side-effect information and utilize these summaries for exeample in an simple abstract interpretation analysis within an existing AOT compiler infrastructure.</p>"},{"location":"#graal-ir-basics","title":"Graal IR Basics","text":"<p>Graal IR is a sea-of-nodes (SoN) representation used by GraalVM compiler. It contains a large amount of nodes. Each node roughly corresponds to a high level instruction such as arithmetic, memory access, or a call.</p> <p>Nodes are roughly divided into two kinds:</p> <ul> <li><code>Fixed</code> nodes are pinned to a specific point in the control-flow order. They are connected to their neighbours via <code>successor</code> edges. <code>Invoke</code>, <code>If</code>, <code>Return</code>, and any node with side effects are typically fixed.</li> <li><code>Floating</code> nodes are constrained only by their data dependencies (<code>input</code> edges), not by any fixed position in control flow. The scheduler is free to place them wherever their inputs are available and their outputs are needed, e.g. an <code>AddNode</code> with no side effects can float to whichever block is most profitable (such as being hoisted out of a loop).</li> </ul> <p>It is worthy mentioning that the more nodes that can be classified as floating rather than fixed, the more freedom the scheduler and later optimization passes have (well at-least this was the raison-d'entre for SoN). <code>Invoke</code> nodes are fixed by default which is one of the two reasons calls end up serialized relative to each other (see \"Why Calls Get Serialized\" below for the other).</p> <p>To demonstrate Graal IR, suppose this Java program:</p> <pre><code>public class Fibonacci {\n    public static int rec(int n, int a, int b) {\n        if (n == 0) {\n            return a;\n        }\n\n        return rec(n - 1, b, a + b);\n    }\n\n    public static int fib(int n) {\n        return rec(n, 0, 1);\n    }\n\n    public static void main(String[] args) {\n        System.out.println(fib(5));\n    }\n}\n</code></pre> <p>The Graal IR of the <code>Fibonacci::rec</code> method is depicted below. The control edges are marked in red, whereas the data edges are marked in blue or black. There is a unique <code>Start</code> node, from which the control flows. The <code>Begin</code> nodes roughly correspond to the beginning of a basic block. One may find it strange that there are two control edges flowing from the <code>Invoke!#Fibonacci.rec</code> node \u2014 this is because <code>Fibonacci.rec</code> is a recursive function, and Graal IR puts a stack-overflow check into the IR, giving two paths from this node: one for normal return, and one for the exception path taken if a <code>StackOverflowError</code> is thrown, in which case we unwind.</p> <p></p> <p>The <code>Invoke!#Fibonacci.rec</code> node above is a concrete instance of a fixed node in the sense described earlier: it sits directly in the <code>successor</code> chain, which is exactly what forces the normal-return and exception paths to be modeled as explicit control edges rather than left floating.</p> <p>To quickly find more detailed examples of Graal IR, I recommend looking at this nice blog post.</p>"},{"location":"#graal-memory-model","title":"Graal Memory Model","text":"<p>Graal tracks memory side effects coarsely using a concept called <code>LocationIdentity</code>. Every node that can write to memory implements either <code>SingleMemoryKill</code> (kills exactly one identity, exposed via <code>getKilledLocationIdentity()</code>) or <code>MultiMemoryKill</code> (kills several, via <code>getKilledLocationIdentities()</code>). Every node that reads from memory implements <code>MemoryAccess</code> and exposes the identity it depends on via <code>getLocationIdentity()</code>, plus a pointer to the last node that could have killed that identity via <code>getLastLocationAccess()</code>/<code>setLastLocationAccess()</code>.</p> <p><code>LocationIdentity</code> forms a small lattice:</p> <ul> <li><code>LocationIdentity.any()</code> \u2014 the top element; conflicts with everything.</li> <li><code>NamedLocationIdentity</code> \u2014 a named \"family\" of locations, e.g. all <code>int[]</code> elements, or a hand-picked identity for a class of internal VM fields.</li> <li><code>FieldLocationIdentity</code> \u2014 one identity per declared field, shared across all instances of that field (i.e. still not per-object, but already narrower than <code>any()</code>).</li> </ul> <p>A memory operation's ordering relative to another is decided purely by whether their identities overlap: two operations with provably disjoint identities may be reordered or interleaved freely; two operations that share (or might share) an identity must stay ordered.</p> <p>The crucial default: an ordinary Java method call (<code>InvokeNode</code>) has no interprocedural information available to the compiler about what it touches, so it is constructed with <code>identity = LocationIdentity.any()</code>. This makes every call a <code>SingleMemoryKill</code> on <code>any()</code>, i.e. a full memory barrier. In the example below, <code>bump(counter)</code>'s write and <code>config.limit</code>'s read \u2014 despite obviously touching disjoint objects \u2014 are therefore always ordered relative to each other in the graph, and neither the scheduler nor floating-read/read-elimination passes are allowed to reorder or interleave memory operations around them.</p> <p>This is a deliberate conservative default: nothing in the Graal compiler currently computes narrower, points-to-informed <code>LocationIdentity</code> values for general method calls. That gap is exactly what this project investigates.</p> <pre><code>class Counter { int value; }\nclass Config { int limit; }\n\nstatic void bump(Counter counter) {\n    counter.value++;\n}\n\nstatic void process(Counter counter, Config config, int n) {\n    for (int i = 0; i &lt; n; i++) {\n        bump(counter);\n        if (i &gt; config.limit) {\n            break;\n        }\n    }\n}\n</code></pre> <p>Semantically, <code>bump(counter)</code> and the read of <code>config.limit</code> are independent of each other because they touch (read from/write to) different objects. Unless the compiler can prove this fact, both instructions are conservatively assumed to affect the same heap state, so Graal introduces a dependency between them. This is very costly, because inside because the <code>process</code> method <code>config.limit</code> never changes across loop iterations, so the read is loop-invariant and should ideally be hoisted out of the loop.But because it's forced to depend on <code>bump</code>'s <code>any()</code> memory kill, it stays pinned inside the loop body.</p> <p>It might seem like the natural fix is to somehow make the <code>Invoke</code> node floating, but this is not what we are proposing, and it isn't possible given how Graal's node hierarchy is structured:</p> <ul> <li><code>FloatingNode</code> is defined with no <code>successor</code> edges at all: it is a plain <code>ValueNode</code>, structurally incapable of having a control-flow successor. <code>InvokeNode</code> (and <code>InvokeWithExceptionNode</code>), by contrast, extend <code>FixedWithNextNode</code>/have mandatory successor edges \u2014 a normal-return path, and for <code>InvokeWithExceptionNode</code>, an exception path as well. A call also carries a <code>FrameState</code> for deoptimization, which must correspond to a specific program point. None of this fits the floating-node contract.</li> <li>There is no <code>FloatingInvokeNode</code> in Graal, and there structurally cannot be one in the current node model: \"floating\" means no fixed control position, which is semantically wrong for an operation that can throw, deoptimize, or otherwise be observed as happening at a specific point relative to other side effects.</li> </ul> <p>What Graal does already support is promoting memory accesses (not calls) from fixed to floating form. This is the actual mechanism we want to reuse/extend:</p> <ul> <li><code>FloatingAccessNode</code> is the floating counterpart of a fixed memory access (e.g. <code>FloatingReadNode</code> vs. <code>ReadNode</code>). A fixed access such as <code>ReadNode</code> exposes <code>canFloat()</code>, and when it can, <code>asFloatingNode()</code> converts it into the equivalent <code>FloatingAccessNode</code>, dropping its position in the <code>successor</code> chain and replacing it with a data dependency on <code>getLastLocationAccess()</code> instead.</li> <li><code>FloatingReadPhase</code> is the compiler phase that performs this conversion. Its <code>FloatingReadClosure.processFloatable</code> walks the fixed-node schedule, and for every <code>FloatableAccessNode</code> that can float, looks up the current \"last known kill\" of its <code>LocationIdentity</code> in a per-location map (<code>MemoryMapImpl</code>), then calls <code>graph.replaceFixedWithFloating(accessNode, floatingNode)</code>. Crucially, every <code>SingleMemoryKill</code>/<code>MultiMemoryKill</code> node it walks past \u2014 including every <code>InvokeNode</code>, since it kills <code>any()</code> by default \u2014 updates (or, in the <code>any()</code> case, clears) this map, which is exactly what pins a read like <code>config.limit</code> to stay after <code>bump(counter)</code> in program order.</li> <li><code>SchedulePhase</code> is the phase that assigns floating nodes to basic blocks once they are floating. It does not decide where fixed nodes go \u2014 a fixed node's block is a mechanical consequence of the <code>successor</code> chain it already sits in (see <code>ControlFlowGraph.renumberBlocks</code>), and <code>SchedulePhase</code> explicitly treats a fixed node's earliest and latest legal block as identical, i.e. no freedom at all. What <code>SchedulePhase</code> actually computes is the earliest/latest legal block for each floating node \u2014 including newly-floated <code>FloatingReadNode</code>s \u2014 based on data dependencies (<code>scheduleEarliestIterative</code>) and, on the latest side, on which blocks are known to \"kill\" the node's <code>LocationIdentity</code> (<code>fillKillSet</code>/<code>checkKillsBetween</code>). A block containing an invoke that kills <code>any()</code> blocks every floating access from being scheduled past it, regardless of what that access actually touches.</li> </ul> <p>So the mechanism we intend to use is: narrow the <code>LocationIdentity</code> that <code>InvokeNode</code>s report, not change their fixed/floating status. <code>InvokeNode</code> already has a constructor overload accepting an explicit <code>LocationIdentity</code> instead of defaulting to <code>any()</code>. If a call's kill identity is narrowed based on points-to information (e.g. tagged with a <code>Counter</code>-heap token instead of <code>any()</code>), then:</p> <ul> <li><code>FloatingReadClosure.processAccess</code>/<code>processCheckpoint</code> will only invalidate the map entry for that specific identity, instead of clearing the whole map \u2014 <code>config.limit</code>'s entry survives across the <code>bump(counter)</code> call.</li> <li><code>SchedulePhase</code>'s kill-set computation no longer treats every block containing <code>bump</code> as killing everything, so the read of <code>config.limit</code> becomes a legal candidate to float, hoist out of the loop, and get scheduled once instead of every iteration.</li> </ul> <p>No changes to <code>FloatingReadPhase</code> or <code>SchedulePhase</code> themselves are required \u2014 both already consume <code>LocationIdentity</code> at the right granularity. The only missing piece is a source of narrower identities for ordinary method calls, which is what the points-to-informed heap-partition analysis in this project aims to provide.</p> <p><code>MemoryAccess</code> represents any node that reads from a memory location. It exposes <code>getLocationIdentity()</code> (which location it reads) and <code>getLastLocationAccess()</code>/<code>setLastLocationAccess()</code> (a pointer to the last node in the memory graph that could have killed that location).</p> <p><code>SingleMemoryKill</code>/<code>MultiMemoryKill</code> represent nodes that write/invalidate one or several <code>LocationIdentity</code>s respectively.</p> <p>The SSU angle: the project's proposal is to break this monolithic memory dependency into fine-grained, partitioned linear tokens (e.g. a <code>Counter</code>-heap token and a separate <code>Config</code>-heap token). Disjoint heap regions then get independent data-flow paths, and the false dependency between <code>bump</code>'s write and <code>config.limit</code>'s read disappears \u2014 unblocking the hoist. The same mechanism addresses three related problems at once:</p> <ul> <li>Breaking the monolithic memory graph. A single write currently invalidates memory state globally for the local optimizer, because Graal can't cheaply tell whether a read and a write touch different heap regions without deep, expensive alias analysis across the whole graph. Partitioned tokens make disjointness explicit and free.</li> <li>Reducing reliance on aggressive inlining. Industrial compilers lean on inlining small functions (getters/setters) specifically to extend the reach of local optimizations \u2014 at the cost of code bloat, worse instruction-cache behaviour, and AOT compile-time overhead. Lightweight interprocedural summaries (input/output side-effect tokens on the function prototype) let Graal optimize a call site without inlining the callee body.</li> <li>Unlocking LICM/CSE/DCE that are currently blocked. Rigid control and memory-ordering edges between operations prevent loop-invariant reads from floating and prevent pure/repeated calls from being deduplicated or removed. Replacing those rigid edges with flexible data-dependency chains lets Graal's existing scheduler reorder and hoist freely.</li> </ul> <p>Key takeaway: the goal is to turn implicit, coarse-grained memory side effects into explicit, fine-grained data dependencies \u2014 letting Graal's existing local optimizer passes (LICM, CSE, DCE) reach their full precision without paying the code-size and compile-time cost of aggressive inlining.</p>"}]}